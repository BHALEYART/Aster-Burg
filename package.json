{
  "name": "aster-trader-board",
  "version": "1.0.0",
  "description": "Aster DEX leaderboard dashboard with Vercel proxy",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "acorn": "^8.16.0",
    "vercel": "^37.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
